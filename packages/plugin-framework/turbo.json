{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "bootstrap": {
      "dependsOn": ["^build"],
      "inputs": [
        "package.json",
        "../../buf.yaml",
        "../proto/**/*.proto",
        "../plugin/protobuf-ts.proto"
      ],
      "outputs": ["src/google/**"]
    }
  }
}
