{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "bootstrap": {
      "dependsOn": ["^build"],
      "inputs": [
        "package.json"
      ],
      "outputs": ["src/google/**"]
    }
  }
}
