syntax = "proto2";
package spec;


message MessageFieldMessage {

    required TestMessage message_field = 1;

    repeated TestMessage repeated_message_field = 2;


    message TestMessage {
        optional string name = 1;
    }

}

message RequestMessage {
  optional string field = 1;
}

message ResponseMessage {
  optional string field = 1;
}
